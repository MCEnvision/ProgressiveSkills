package com.envisione.progressiveskills.common.network;

/** Payload families that use the shared bounded compressed transfer envelope. */
public enum TransferKind {
    DEFINITIONS,
    FULL_STATE
}
