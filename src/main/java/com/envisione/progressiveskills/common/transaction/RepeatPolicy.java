package com.envisione.progressiveskills.common.transaction;

/** Phase 4 receipt policies supported by the lifecycle core. */
public enum RepeatPolicy {
    ALWAYS,
    ONCE_PER_TRANSACTION,
    ONCE_PER_CHARACTER
}
