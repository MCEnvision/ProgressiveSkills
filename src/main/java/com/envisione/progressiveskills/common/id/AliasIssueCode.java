package com.envisione.progressiveskills.common.id;

/** Stable local validation categories for replacement aliases. */
public enum AliasIssueCode {
    TOO_MANY_ALIASES,
    CROSS_KIND,
    SELF_ALIAS,
    CONFLICTING_TARGET,
    CYCLE,
    CHAINED_ALIAS,
    CHAIN_TOO_LONG
}
